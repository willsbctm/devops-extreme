apiVersion: v1
kind: Service
metadata:
  name: {{ include "minnie2.fullname" . }}
  labels:
    {{- include "minnie2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "minnie2.selectorLabels" . | nindent 4 }}
